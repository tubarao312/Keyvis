services:
  client:
    build:
      context: .
      target: development  # Use the 'development' stage from Dockerfile.prod for development
    command: npm run dev
    env_file:
      - .env
    ports:
      - 3000:3000
    volumes:
      - .:/app

  